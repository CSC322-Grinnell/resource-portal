-# categories#show
-# Displays the "contents" of a category, i.e. its resources
.container
  .row
    .col-md-12
      %h1{:style => "text-align: center"} #{@category.category_name}

- if !@resources.empty?
  .container{:id => "all-resources"}
    - @resources.each do |resource|
      = render :partial => 'resources/resource', :locals => { :resource => resource }
  %hr
