-# categories#show
-# Displays the "contents" of a category, i.e. its resources
.container
  .row
    .col-md-12
      %h1{:style => "text-align:center"} #{@category.name}
  - if !@category.tag_list.blank?
    .row
      .col-md-12
        %h4{:style => "text-align:center"}
          To refine your search further, select a more specific tag:
          - @category.tag_list.each do |tag|
            = link_to tag + " --", tag_path(tag)

- if !@resources.empty?
  .container{:id => "all-resources"}
    - @resources.each do |resource|
      = render :partial => 'resources/resource', :locals => { :resource => resource }
  %hr
